let calculator = {
    add: function (a, b) {
        return a + b;
    },
    substract: function (a, b) {
        return a - b;
    },
    oneHundred: 100,
    arrays: ["sigit", "sinta", "budi"],
};

export { calculator };
